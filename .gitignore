# ---- browser scratch directories created by the test drivers ----
tests/.chrome-profile/
tests/.chrome-probe/
tests/.chrome-glsl/
tests/.chrome-boot/
tests/.chrome-shot/
tests/.tmpprof/
tests/.composed.txt
tests/.scenefrag.glsl
tests/.composed.frag

# ---- captured frames ----
# 32 MB of PNGs. They are evidence for tests/TEST-REPORT.md, not source and not
# part of the site; the site itself is staged into docs/ without them.
# Regenerate any time with:  npm run shots
tests/shots/

# ---- nothing here is generated by a build, because there is no build ----
node_modules/
*.log
.DS_Store
Thumbs.db
